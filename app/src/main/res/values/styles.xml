<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="chipIconTint">@color/yellow</item>
    </style>

    <style name="ToolbarTheme" parent="@style/ThemeOverlay.AppCompat.ActionBar">
        <!-- Customize color of navigation drawer icon and back arrow -->
        <item name="colorControlNormal">@color/colorPrimaryDark</item>
    </style>

    <style name="button" parent="@style/Widget.AppCompat.Button">
        <item name="android:background">@drawable/rounded_button_login</item>
    </style>

    <style name="tabStyle">
        <item name="android:background">@drawable/rounded_button_login</item>
        <item name="tabRippleColor">@color/white</item>
        <item name="tabTextAppearance">@color/white</item>
        <item name="tabTextColor">@color/white</item>

    </style>

</resources>
